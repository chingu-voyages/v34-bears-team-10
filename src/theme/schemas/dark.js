/**
 * Dark 'palette' is the same as the `light` one for now
 * we might want to change this if we add theming at
 * a later point.
 */
export const darkSchema = {
  primary: {
    light: "#ffffff",
    main: "#ffffff",
    dark: "#cccccc",
  },
  secondary: {
    light: "#5c5c5c",
    main: "#333333",
    dark: "#0c0c0c",
  },
};

export default darkSchema;
